# Production build env for Vite (baked into the JS bundle at build time).
# Copy:
#   cp .env.production.example .env.production
#
# Requests become: https://www.scaralphaai.com/api/...
# (no trailing slash)

VITE_API_BASE_URL=https://www.scaralphaai.com
